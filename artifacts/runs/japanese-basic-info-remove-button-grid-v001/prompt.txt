[TASK]
Edit the exact Japanese 基本情報 window reference. Remove only the eight right-side page buttons and their English labels: status, option, items, equip, skill, map, chat, friend. Fill that rectangular right body region with the same uninterrupted white window-body surface. Preserve everything else exactly.

[REFERENCE ROLE]
Reference image 1 is the immutable 280 by 120 source crop and fixes every coordinate and all unchanged pixels.

[PRESERVATION INVARIANTS]
- Do not alter the title bar, Japanese title, identity text, HP and SP rows, meters, level panel, footer, window border, palette, or crop.
- Keep the source's late-1990s low-resolution raster styling and exact proportions.
- This is a local object-removal pass, not a redraw.

[CANVAS AND LAYOUT]
- Return the same complete 280 by 120 window crop at exactly 1120 by 480 pixels with no added margin.
- The changed region is x=205..277, y=19..98 at source scale; outside it is Exact Copy.

[REGION EDITS]
- REGION: right page-button grid
  CHANGE: Remove all eight complete beveled buttons and every glyph inside them. Reconstruct only the continuous plain white body surface and preserve the right border.
  PRESERVE: outer right frame; white body color; all pixels outside the declared region

[STYLE SYSTEM]
- Exact source-matched Japanese PC RPG raster UI
- hard pixel-scale edges and pale blue-grey chrome

[ASSET RULES]
- No button silhouette, label, ghost glyph, pseudo-text, replacement control, or magenta desktop in the changed region.

[NEGATIVE CONSTRAINTS]
- No redesign, modern UI, smoothing, rounded cards, added text, new icon, changed scale, changed border, or whole-screen reinterpretation.

[QUALITY GATE]
- The eight buttons are absent.
- The exposed white panel is continuous.
- All unrelated source regions remain aligned.
